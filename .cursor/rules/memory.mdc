---
description: Project memory — automatically loaded into every conversation
alwaysApply: true
---

# Project Memory — InstaGold (Gold)

> **Read `ARCHITECTURE.md` for the full system design.** This file is a quick reference.

## App Identity
- App name: **InstaGold**
- Android package (applicationId): `com.ibrahym.goldfamily` (do NOT change)
- Android namespace: `com.ibrahym.instagold`
- iOS bundle ID: `com.ibrahym.goldtracker`
- Firebase project: `goldcalculate`
- **Build commands ALWAYS require `--flavor dev|prod`** for Android

## Architecture (May 2026)
- Monorepo: `scraper-service/` (dormant), `main-backend/` (LIVE on Oracle Cloud), `flutter-app/`
- **Backend is LIVE** at `https://207.127.99.147.nip.io` — Oracle Cloud Always Free VM (Jeddah region)
- Scraper merged directly into `main-backend/src/scraperClient.js` — no separate scraper service needed
- Mobile mode: local SQLite for most CRUD + direct eDahab scraping for prices
- **Price alerts and FCM device registration go through the backend via HTTP** — NOT local SQLite
- Web mode: talks to main-backend HTTP APIs

## Backend Hosting — Oracle Cloud (May 2026)
- **VM**: `instagold-backend` — VM.Standard.E2.1.Micro (Always Free, AMD, 1 OCPU, 1GB RAM)
- **IP**: `207.127.99.147`
- **Region**: Saudi Arabia West (Jeddah) — `me-jeddah-1`
- **OS**: Ubuntu 22.04
- **HTTPS**: Caddy reverse proxy with auto-renewing Let's Encrypt cert via `nip.io`
- **Process Manager**: PM2 (auto-starts on reboot via systemd)
- **Domain**: `207.127.99.147.nip.io` (resolves to the VM IP)
- **Ports open**: 22 (SSH), 80 (HTTP→HTTPS redirect), 443 (HTTPS)
- **Port 3000**: Closed externally — Express only accessible via Caddy on localhost
- **SSH access**: `ssh ubuntu@207.127.99.147` (key: `~/.ssh/id_rsa`)
- **Code location on VM**: `/home/ubuntu/Gold/main-backend/` (branch: `develop`)
- **Env file**: `/home/ubuntu/Gold/main-backend/.env`
- **OCI CLI configured**: `~/.oci/config` with API key at `~/.oci/oci_api_key.pem`
- **Tenancy OCID**: `ocid1.tenancy.oc1..aaaaaaaai4rgyo36efj25jplnir35kthxfasn7gbdcj2yxlvr3ocvawxp3tq`
- **User OCID**: `ocid1.user.oc1..aaaaaaaaevrqlls54crj3m3sodokdi4pi7gky7vzzvp5hboh5za3mwqqljoa`
- **Instance OCID**: `ocid1.instance.oc1.me-jeddah-1.anvgkljrvexgrnice7ljovvzo6dgl6fvw6k5wqi5wns7gcp5k7wyk7oymf4a`
- **Previous host**: Railway (`backend-production-c042.up.railway.app`) — trial expired, migrated to Oracle Cloud

### Deploying backend updates:
```bash
ssh ubuntu@207.127.99.147 "cd /home/ubuntu/Gold && git pull origin develop && cd main-backend && npm install --production && pm2 restart instagold-backend"
```

### Key env vars on VM (.env):
```
PORT=3000
FIREBASE_PROJECT_ID=goldcalculate
FCM_SUMMARIES_ENABLED=true
MIN_FCM_CLIENT_BUILD=3
RENDER_EXTERNAL_URL=https://207.127.99.147.nip.io
FIREBASE_SERVICE_ACCOUNT_JSON=<full JSON string>
```

## Notifications — How They Actually Work
- **FCM is ACTIVE** — Oracle Cloud backend sends push at Cairo 07:00/11:00/15:00/19:00 via `notificationsScheduler.js`
- `FCM_SUMMARIES_ENABLED=true` and `MIN_FCM_CLIENT_BUILD=3` in backend env
- `push_notifications_service.dart` registers devices with backend on first launch
- `isFcmActive()` returns true when backend confirms — local notifications self-disable
- **Local notifications are FALLBACK only** — they auto-activate if backend is unreachable
- **iOS before Apple Developer/APNs:** do NOT show foreground notification banners on app open/resume. App open/resume only refreshes prices + widget data. iOS local notifications come from `ios_background_fetch.dart` only, and only when iOS wakes the app and prices changed meaningfully.
- **iOS widget:** best-effort only while app is closed. It refreshes immediately on app open/resume and on iOS background-fetch wakes; do not promise real-time widget updates without APNs/FCM.
- **Price threshold alerts** — user creates via bell icon, backend checks on price sync, sends FCM push
- Two Android channels: `price_updates` (daily, mutable) and `price_alerts` (threshold, max priority)

## Key Data Flow Rules
| Data | Mobile source | Backend involved? |
|---|---|---|
| Prices for display | Local `GoldScraper` + SQLite | No |
| Members, assets, savings, goals | Local SQLite | No |
| Price alerts CRUD | HTTP to Oracle Cloud backend | **Yes** |
| FCM device registration | HTTP to Oracle Cloud backend | **Yes** |
| Slot notifications | FCM from Oracle Cloud (primary) / local (fallback) | **Yes** |

## STRICT Design Rules (MANDATORY)

### Logo
- Use ONLY the provided IG monogram image (`assets/icons/ig_logo.png`)
- Logo in: app header, login screen, splash, empty states, launcher icon

### Dark Mode (must NOT be flat)
- Base: `#0B0B0D` with layered gradients
- Wave patterns via `PremiumBackground` widget
- Radial gold glow highlights
- **FAIL condition: flat black background = WRONG**

### Gold Styling
- Primary: `#D4AF37`, deep `#C9A227`, light `#E8CD5A`, muted `#B8962E`
- Always gradient gold, never flat

### Language Rules
- Never use "الحية" (live) or "اللحظية" (instant) anywhere in any string
- Use "عيار" for karat (not "K")
- Use "الأونصه" for ounce (not "الأوقية")
- Use "أسعار الذهب" (not "أسعار الذهب الحية")

## Key Files
- Config: `flutter-app/lib/config.dart` (apiBaseUrl → Oracle Cloud HTTPS)
- Dashboard: `flutter-app/lib/screens/dashboard_screen.dart`
- Tutorial: `flutter-app/lib/screens/tutorial_screen.dart`
- Price alerts: `flutter-app/lib/screens/price_alerts_screen.dart`
- API: `flutter-app/lib/services/api_service.dart`
- FCM: `flutter-app/lib/services/push_notifications_service.dart`
- Local notifs: `flutter-app/lib/services/notifications_service.dart`
- Price watcher: `flutter-app/lib/services/price_watcher.dart`
- iOS bg fetch: `flutter-app/lib/services/ios_background_fetch.dart`
- Analytics: `flutter-app/lib/services/analytics_service.dart`
- Update: `flutter-app/lib/services/update_service.dart`
- Database: `flutter-app/lib/services/database_helper.dart` (version 3)
- Scraper: `flutter-app/lib/services/gold_scraper.dart`
- App entry: `flutter-app/lib/app.dart` (tutorial flow)
- MainActivity: `flutter-app/android/app/src/main/kotlin/.../MainActivity.kt`
- Backend entry: `main-backend/src/index.js`
- Backend scraper: `main-backend/src/scraperClient.js` (merged, scrapes edahabapp.com directly)
- Backend alerts: `main-backend/src/notificationsService.js`
- Backend FCM: `main-backend/src/notificationsScheduler.js`
- Backend DB: `main-backend/src/db.js`
- Backend config: `main-backend/src/config.js`
- iOS widget: `flutter-app/ios/InstaGoldWidget/InstaGoldWidget.swift`
- Android widget: `flutter-app/android/app/src/main/kotlin/.../InstaGoldWidgetProvider.kt`
- L10n: `flutter-app/lib/l10n.dart`
- Theme: `flutter-app/lib/theme/app_themes.dart`

## Features Implemented (June 2026)
- First-launch tutorial (6 slides, skippable, shown once via SharedPreferences)
- Hide/show assets toggle (masks monetary values + total grams across Assets tab and Home tab)
- 2.5g ingot weight option
- Improved USD/EGP rate (scraped from edahabapp.com, fallback to open.er-api.com)
- Member selector as subtitle under InstaGold wordmark
- Gold calculator uses sell rate (goals section only)
- Reduced keyboard spacing in calculator inputs
- Adaptive icon XML for Android (proper rendering on MIUI/Samsung)
- Price parsing validation (>500 EGP for karats, >1000 for gold pound, 500-15000 for ounce)
- Telegram fallback regex fix (skip karat digit, require 3+ char numbers)
- Ingots & Coins calculator (24K ingots + 21K coins, 8 manufacturers, manufacturing/cashback/resale breakdown)
- Tutorial slide 7 for ingots/coins feature
- Goals use sell price (not buy price) for target calculations
- Firebase Analytics integration with 25+ custom events tracking all feature usage
- In-app update notification system (pre-update dialog + post-update What's New popup)

## Savings/Goals Features
- Weight inputs use `numberWithOptions(decimal: true)` — decimal grams work
- `PurchaseGoals` DB has `manufacturing_price_g REAL DEFAULT 0` column (DB version 4)
- Gold Calculator `ExpansionTile` between savings and goals sections
- Calculator uses SELL rate (not buy rate) for goals section
- Taxes/tariff: fixed 10 EGP/gram (not percentage)
- Arabic label for adds: "الضريبة/الدمغة والمصنعية"

## Play Store Status (May 2026)
- App is **applying for Production access** — application submitted May 21, 2026
- Package: `com.ibrahym.goldfamily`
- Play Console: https://play.google.com/console/u/0/developers/6183037720371974289/app/4972005972149303949
- Closed testing completed: 12 testers, 14+ days
- **Latest version in production**: `1.0.4+10` — live on Google Play
- **Codebase HEAD**: `1.0.5+13` — ready for next Play Store release
- Google Play signing SHA-1: `F3:13:36:92:3D:FD:83:F9:33:87:D1:30:EF:44:52:E1:70:36:95:B4` — already in Firebase
- `play-store-assets/` folder contains all graphics (icon, feature graphic, 5 phone + 4 tablet screenshots)
- Ads: **disabled** (`kAdsEnabled = false` in `lib/config/ad_config.dart`) — re-enable when downloads reach suitable numbers

## Version History
| Version | Code | Status | Notes |
|---|---|---|---|
| 1.0.1+3 | v3 | Was in closed testing | Stable baseline used by testers |
| 1.0.1+4 | v4 | develop branch | Added tutorial, pending notes |
| 1.0.1+5 | v5 | skipped | — |
| 1.0.1+6 | v6 | uploaded to testing | Icon fix, member dialog fix |
| 1.0.2+7 | v7 | closed testing | Full develop merge: HTTPS, tutorial, all fixes |
| 1.0.3+8 | v8 | superseded | Goals use sell price fix |
| 1.0.4+10 | v10 | **Production (live)** | Ingots & coins calculator, tutorial slide 7, permission fix |
| 1.0.4+12 | v12 | codebase HEAD | Removed invoice attachment feature |
| 1.0.5+13 | v13 | codebase HEAD | In-app update system, analytics |

## Git
- Remote: github.com/ibrahymsa3ed/Gold
- Branches: `main` (production), `develop` (development)
- `main` and `develop` are now in sync (merged May 21, 2026)
- `InstaGold.apk` / `InstaGold.aab` at repo root are gitignored

## General Rules (always apply)
- For **every new change**: build a pre-release (debug) APK, test on emulator AND physical Android device, confirm working before finalizing
- Never use "الحية" or "اللحظية" in any string
- Keep `main` clean and stable — develop on `develop` branch

## iOS Device Install Notes
- No paid Apple Developer account yet — can only install via USB + Xcode
- New devices need `xcodebuild -destination 'id=<ID>' -allowProvisioningUpdates` first
- Each new device needs Developer Mode enabled (Settings > Privacy & Security)
- Use `xcrun devicectl list devices` to find device IDs
- Your iPhone 16 Pro Max: `620092AA-C957-55BC-B0BD-A62D6D05AD8F` (iOS 26.5)
- Eman's iPhone 13: `DFCFAB94-8A50-50BF-A870-A6CA511347C7`
- iPad: `4EB3A29C-784D-5FB5-8C40-E1F735F709D0`

## Accounts & Services
- **Firebase**: project `goldcalculate` — Firebase Auth, FCM, Analytics, service account key at repo root
- **Google Play Console**: developer ID `6183037720371974289`
- **Oracle Cloud**: tenancy `ibrahymsaaeed`, region `me-jeddah-1` (Jeddah)
- **GitHub**: `ibrahymsa3ed/Gold`
- **ExchangeRate-API**: used for USD/EGP fallback (free tier, 1h cache)
- **Render.com**: account created but card verification failed — NOT in use
- **Railway**: trial expired — migrated to Oracle Cloud
