---
description: Project memory — automatically loaded into every conversation
alwaysApply: true
---

# Project Memory — InstaGold (Gold)

## App Identity
- App name: **InstaGold**
- Android package (applicationId): `com.ibrahym.goldfamily` (Play Store published — do NOT change)
- Android namespace: `com.ibrahym.instagold`
- iOS bundle ID: `com.ibrahym.goldtracker` (matches Firebase `GoogleService-Info.plist`)
- Platforms: **Android + iOS**
- APK outputs at repo root: **`InstaGold.apk`** (prod) via `flutter build apk --flavor prod`
- AAB: `InstaGold.aab` at repo root
- IPA: `InstaGold.ipa` at repo root (development signed, Personal Team Y6S29VSUJ7, 7-day expiry)
- Play Store bundle: `./scripts/build-play-aab.sh` → `app-prod-release.aab`
- Play prep: `PLAY_STORE_PREP.md`; owner checklist `YOUR_ACTIONS_BEFORE_PLAY.md`; privacy draft `docs/PRIVACY_POLICY_TEMPLATE.md`; `scripts/print-release-signing-fingerprints.sh` for Firebase SHA
- Firebase project: goldcalculate
- **Build commands ALWAYS require `--flavor dev|prod`** for Android — never build without flavor
- iOS signing: Personal Team (Y6S29VSUJ7), `CODE_SIGN_STYLE = Automatic`, `ExportOptions.plist` uses `development` method
- Physical iPhone install: open `ios/Runner.xcworkspace` in Xcode → select iPhone 16 Pro Max → Cmd+R

## Architecture
- Monorepo: `scraper-service/` (price scraping), `main-backend/` (API), `flutter-app/` (mobile UI)
- Mobile mode: local SQLite + direct scraping — no backend servers needed on device
- Web mode: talks to main-backend HTTP APIs
- No CI/CD pipelines

## STRICT Design Rules (MANDATORY — never skip)

### Logo
- Use ONLY the provided IG monogram image (`assets/icons/ig_logo.png`) — NEVER use Material icons as brand placeholder
- Logo MUST appear in:
  - App header (top-left, size 36+)
  - Login screen (centered, 100x100, gold glow shadow)
  - Splash screen (Android native mipmap `launch_image`)
  - Empty states (subtle, low opacity ~0.35)
  - Android launcher icon (all densities via `flutter_launcher_icons`)

### Dark Mode (CRITICAL — must NOT be flat)
- Base: `#0B0B0D` with layered gradients using deep gold/brown tones (`#12100C`, `#14110D`)
- Vertical warm gradient overlay with gold-tinted bands
- **Visible flowing wave patterns** — gold/dark-gold tones, multiple flowing curves, `CustomPaint` in `PremiumBackground` widget
- Radial gold glow highlights behind key areas (headers, cards)
- Section cards use gradient dark background (`#1C1916` → `#181614`), gold border `0.10` alpha
- **FAIL condition: flat black background = WRONG**

### Gold Styling
- Primary accent: `#D4AF37`, deep `#C9A227`, light `#E8CD5A`, muted `#B8962E`
- Always use **gradient gold** — never flat gold
- Price cards: 3-4 stop gold gradient, soft glow `boxShadow`, rounded corners 20px
- Buttons: gold gradient (`#E8CD5A → #D4AF37 → #C9A227`), glow shadow, dark text on button
- `FilledButton` and `ElevatedButton` theme: gold background with dark foreground

### Cards & UI
- Cards: gradient gold for price cards, gradient dark for section cards
- Soft glow or elevation (gold-tinted shadow in dark mode)
- Rounded corners (20-22px)
- Gold accent gradient bar on section headers

### Navigation
- Floating glassmorphism pill-shaped bottom nav
- Dark glass surface in dark mode (`kDarkCard` at 0.85 alpha)
- Gold border (0.08 alpha) in dark mode
- `BackdropFilter` blur (sigmaX/Y: 20)
- Gold active indicator and label

### Background Widget
- `PremiumBackground` wraps both `DashboardScreen` and `LoginScreen`
- Only active in dark mode; light mode passes child through
- Located at `lib/widgets/premium_background.dart`

## UI Design Decisions
- Price cards: gold gradient rounded rectangles, drag-and-drop reorderable
- Home layout: 21K full-width hero → 24K full-width hero → 14K+18K row → Pound+Ounce row
- Gap card: full-width tinted box (green/red), EGP gap value large and centred, tappable for explanation popup
- Ounce card: shiny gold gradient
- Asset cards: icon in gold circle (ClipOval for PNGs), title, karat/weight/date subtitle, Purchase/Current/Profit rows with %
- Empty states: IG logo at low opacity + gold gradient "Add" button
- No custom ClipPath shapes — standard rounded containers only

## Code Patterns
- Dialog builders: always use builder context (`ctx`) for AppStrings, Theme, Navigator — never State's `context`
- Member: name field only, no relation field. API `addMember`/`updateMember` have optional relation param
- Localization: `AppStrings.t(context, 'key')` — Arabic and English
- Colors: `withValues(alpha: ...)` not deprecated `withOpacity`
- Theme variant: `kUiDesignVariant` in `ui_design_variant.dart` — set to `classic` to rollback

## Key Files
- Theme: `flutter-app/lib/theme/app_themes.dart`
- Dashboard: `flutter-app/lib/screens/dashboard_screen.dart`
- Login: `flutter-app/lib/screens/login_screen.dart`
- IG Logo widget: `flutter-app/lib/widgets/ig_logo.dart`
- Premium background: `flutter-app/lib/widgets/premium_background.dart`
- API: `flutter-app/lib/services/api_service.dart`
- L10n: `flutter-app/lib/l10n.dart`
- Design variant: `flutter-app/lib/theme/ui_design_variant.dart`
- IG logo asset: `flutter-app/assets/icons/ig_logo.png`
- iOS widget: `flutter-app/ios/InstaGoldWidget/InstaGoldWidget.swift`
- iOS widget asset catalog: `flutter-app/ios/InstaGoldWidget/Assets.xcassets/`
- Android widget layout: `flutter-app/android/app/src/main/res/layout/instagold_widget.xml`
- Android widget provider: `flutter-app/android/app/src/main/kotlin/com/ibrahym/instagold/InstaGoldWidgetProvider.kt`

## Workflow After Each Edit (MANDATORY)
### Android
1. Build **prod APK**: `flutter build apk --release --flavor prod --dart-define=INSTAGOLD_FLAVOR=prod`
2. Build **prod AAB**: `flutter build appbundle --release --flavor prod --dart-define=INSTAGOLD_FLAVOR=prod`
3. Copy outputs: `InstaGold.apk` and `InstaGold.aab` to repo root
4. Install APK on emulator: `adb install -r` (uninstall first if signature mismatch)
5. Launch on emulator: `adb shell monkey -p com.ibrahym.goldfamily -c android.intent.category.LAUNCHER 1`

### iOS
6. Build iOS simulator: `flutter build ios --simulator`
7. Install on simulator: `xcrun simctl install booted build/ios/iphonesimulator/Runner.app`
8. Launch on simulator: `xcrun simctl launch booted com.ibrahym.goldtracker`
9. Build IPA: `flutter build ipa --release --export-options-plist=ios/ExportOptions.plist`
10. Copy IPA: `InstaGold.ipa` to repo root

### Finalize
11. Git commit & push
12. **Do NOT** build dev APK unless user explicitly asks
13. **Do NOT** upload to Drive unless user explicitly asks
14. Upload APK/IPA to Google Drive (`rclone copyto ... "gdrive:InstaGold Releases/..."`) only when user asks

## Settings Persistence
- Uses `shared_preferences` to cache theme (dark/light) and locale (en/ar) at app level
- On change: `SharedPreferences` is updated immediately in `_handleThemeChanged` / `_handleLocaleChanged` in `app.dart`
- On startup: `_loadPersistedSettings()` reads from SharedPreferences before showing any screen
- Also persisted to SQLite `UserSettings` table via `apiService.updateSettings()`

## Login Screen
- Always uses dark premium background regardless of theme (`PremiumBackground(forceDark: true)`)
- Gold gradient login button, gold-bordered input fields, top gradient header
- Matches the premium luxury dark theme reference design

## Git
- Remote: github.com/ibrahymsa3ed/Gold
- Branch: main
- Always update `ARCHITECTURE.md` and `README.md` with code changes
