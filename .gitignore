# Root secrets
*firebase-adminsdk*.json
google-services.json
GoogleService-Info.plist
*.p12
*.key
# Android Play upload signing (never commit)
flutter-app/android/key.properties
flutter-app/android/*.jks
flutter-app/android/*.keystore

# IDE
.idea/

# Generic
.DS_Store

# Old APK builds
FamilyGold.apk
GoldFamily.apk
# Local dev flavor artifact (rebuild with ./scripts/build-dev-apk.sh)
InstaGold-dev.apk
# Current prod release artifacts (build to repo root locally; do not commit)
InstaGold.apk
InstaGold.aab
flutter-app/android/.kotlin/

# Firebase Hosting deploy cache (local only)
flutter-app/.firebase/

# Claude Code worktrees
.claude/worktrees/
